# This is a hand-created yaml file for the functional testing of the OCI-sync tool. If the repos change this file
# will probably need to be modified. If the registry.redhat.io user or password changes - modified.
registry.redhat.io:
  credentials:
    username: 11009103|registry-clone
    password:
  tag-options:
    filtertags: ^(latest|v?(\d+\.?){2,3}(-\d+)?(-p\d)?)$
  images:
    ubi7/ubi:
